---
- hosts: all
  become: true
  vars:
    user_names:
      - paul
      - ruby
  tasks:
    - name: Remove Users
      user:
        name: "{{ item }}"
        state: absent
        remove: yes
        loop: "{{ user_names }}"
